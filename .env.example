# Application Settings
DEBUG=false
ENVIRONMENT=development

# Database Settings
# For Docker: use port 5433 (mapped from container's 5432)
# Note: The app will automatically convert this to postgresql+asyncpg:// for async operations
DATABASE_URL=postgresql://calendar_user:calendar_pass@localhost:5433/calendar_db
DATABASE_POOL_DISABLED=false
DATABASE_ECHO=false

# Server Settings
HOST=0.0.0.0
PORT=8000

# Security Settings
# Add API keys here when implemented
